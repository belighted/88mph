%h1 Project

%div{:class => "form-a", :id => "project-new"}
  -form_for @project, :url => projects_path do |f|
    = f.error_messages
    = render :partial => "form", :object => f
    %p.form-submit
      %span.button= f.submit "Create project"