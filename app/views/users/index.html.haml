%section#head-a
  =image_tag("missing_thumb.png", :alt => "Default Avatar")
  %h2= @current_company
  %p= link_to @current_company.domain, "http://www.#{@current_company.domain}"
  = render :partial => "_shared/invite"
%h1 Members of the team
%section
  %ol#member-list
    - @users.each do |u|
      %li
        = link_to(u,u)
        %em= u.role
        %p.action-link= link_to image_tag("delete.png"), u unless u == current_user
